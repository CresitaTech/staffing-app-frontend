import { LimitUptoTwoDIgitsDirective } from './limit-upto-two-digits.directive';

describe('LimitUptoTwoDIgitsDirective', () => {
  it('should create an instance', () => {
    const directive = new LimitUptoTwoDIgitsDirective();
    expect(directive).toBeTruthy();
  });
});
