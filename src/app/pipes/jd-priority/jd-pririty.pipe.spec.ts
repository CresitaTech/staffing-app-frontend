import { JdPrirityPipe } from './jd-pririty.pipe';

describe('JdPrirityPipe', () => {
  it('create an instance', () => {
    const pipe = new JdPrirityPipe();
    expect(pipe).toBeTruthy();
  });
});
