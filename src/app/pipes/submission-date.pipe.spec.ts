import { SubmissionDatePipe } from './submission-date.pipe';

describe('SubmissionDatePipe', () => {
  it('create an instance', () => {
    const pipe = new SubmissionDatePipe();
    expect(pipe).toBeTruthy();
  });
});
