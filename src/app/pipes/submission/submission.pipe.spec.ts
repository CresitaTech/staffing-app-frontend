import { SubmissionPipe } from './submission.pipe';

describe('SubmissionPipe', () => {
  it('create an instance', () => {
    const pipe = new SubmissionPipe();
    expect(pipe).toBeTruthy();
  });
});
